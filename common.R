cbind(x, y, w)
as.data.frame(xy) # convert object into dataframe
data(RandHIE) # load in dataset from library

rbinom(n_trials, n, p)
rnorm(n_trials, mu, sigma)
runif(n_trials, a, b)

cor(cbind(x, y, w))
